# Bootstrap state — contains discovered IPs and passwords; never commit
bootstrap-state.kv
bootstrap-state.json

# Live cluster config — contains BMC IP; gitignored so it stays local
bootstrap-config.kv
bmc-manifest.kv
images-manifest.kv
image-cache/

# Old/superseded scripts
_bootstrap-*.exp

# kubectl binary (too large; users install their own)
kubectl
kubectl.sha256

# Generated TLS artefacts
registry-certs/
*.key
*.crt
*.csr
*.srl
*.cnf
htpasswd

# Editor / OS
.DS_Store
*.swp
*~
