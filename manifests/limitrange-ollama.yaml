apiVersion: v1
kind: LimitRange
metadata:
  name: ollama-defaults
  namespace: ollama
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: "100m"
        memory: "256Mi"
      default:
        cpu: "1"
        memory: "4Gi"
      max:
        memory: "28Gi"
